<% if (Array.isArray(data) && data.length > 0) {%>
<div class="player-pane assists">
    <ul class="header">
        <li class="player">球员</li>
        <li class="team">球队</li>
        <li class="data">助攻</li>
    </ul>
    <ul class="list">
        <% for(var i = 0, len = data.length; i < len; i ++) {%>
        <% var item = data[i] %>
        <li class="item">
            <div class="player">
                <div class="rank">
                    <%= item.rank %>
                </div>
                <div class="name">
                    <%= item.playerName %>
                </div>
            </div>
            <div class="team">
                <%= item.playerInfo.teamInfo.teamName %>
            </div>
            <div class="data"><%= item.stats.assists %></div>
        </li>
        <% } %>
    </ul>
    <div class="more">查看更多</div>
</div>
<% } else { %>
    <div class="tip">助攻榜暂时为空……</div>
<% } %>